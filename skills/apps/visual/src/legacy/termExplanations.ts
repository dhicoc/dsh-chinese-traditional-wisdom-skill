/**
 * termExplanations — 纯 TS 术语通俗解释（从 visual/js/core.js 抽取，零 window 依赖）
 */

import terms from './termExplanations.json';

const BAZI_TERM_MAP: Record<string, string> = {
  日主: '八字中以日柱天干代表本人，是观察其他干支关系的参照点。可以先把它理解成“命盘里的自己”。',
  十神: '以日主为参照，把其他干支之间的关系分为十种类型，例如资源、表达、财富、规则与同伴。它们是分析关系的分类工具。',
  喜用神: '八字中用于帮助整体五行趋于平衡的参考元素。“喜神”指有助平衡的元素，“用神”指其中更优先参考的一项；不同流派取法可能不同。',
  格局: '根据出生季节与干支关系归纳出的结构类型，用来帮助观察命盘重点；不同流派的命名和判断标准可能不同。',
  身强: '指日主在命盘中得到的支持相对较多，力量偏强。这是平衡判断，不等同于“好”或“坏”。',
  身弱: '指日主得到的支持相对较少，力量偏弱。这是平衡判断，不等同于“差”。',
  月令: '出生月份所代表的季节力量，是判断五行强弱的重要背景。',
  得令: '日主五行与出生季节较相合，通常表示更容易得到季节力量的支持。',
  失令: '日主五行与出生季节不完全相合，通常表示季节给予的支持相对较少。',
  通根: '日主五行在地支藏干中有对应来源，表示它并非孤立出现。',
  扶抑: '根据整体偏强或偏弱，选择“补足”或“疏泄”的平衡思路。',
  调候: '观察季节偏寒、偏热、偏燥或偏湿，并寻找让整体更协调的元素。',
  通关: '当两种五行冲突明显时，寻找能在两者之间起缓冲作用的元素。',
  病药: '传统命理对某些组合的比喻：把失衡点叫“病”，把调节方式叫“药”，不指医学诊断。',
  从格: '少见的特殊结构。当日主力量极弱、全局力量高度集中时，才会考虑按从格观察。',
  化气: '日干与相邻天干相合，并得到足够五行支持时，才会考虑的进阶观察。',
  本命盘: '根据出生时间排出的四柱，是后续观察大运和流年的基础。',
  冲合刑害: '干支之间常见的互动关系。它们提示不同位置之间存在呼应、变化或牵制，需结合整体理解。',
  太一天符: '《素问·六微旨大论》有“天符岁会……太一天符之会也”之说。它标识天符与岁会同时成立的传统运气格局，仅作传统文化与气候病机理论学习参考，不构成医学诊断或治疗建议。',
};

const TERM_MAP = { ...(terms as Record<string, string>), ...BAZI_TERM_MAP };

/** 查询单个术语的通俗解释 */
export function explainTerm(term: string): string {
  if (!term) return '暂无该术语的解释';
  const direct = TERM_MAP[term];
  if (direct) return direct;
  for (const [key, val] of Object.entries(TERM_MAP)) {
    if (term.includes(key) || key.includes(term)) return val;
  }
  return '暂无该术语的解释';
}

export function hasTermExplanation(term: string): boolean {
  return explainTerm(term) !== '暂无该术语的解释';
}
